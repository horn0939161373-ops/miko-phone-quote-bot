name = "miko-liff-subscribe"
main = "src/index.js"
compatibility_date = "2024-11-01"

# GITHUB_TOKEN 跟 LIFF_CHANNEL_ID 不要寫在這裡（這個檔案會進 git）。
# 部署後用以下指令設定成 Secret：
#   wrangler secret put GITHUB_TOKEN
#   wrangler secret put LIFF_CHANNEL_ID
